<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/LoginActivity">

    <fragment
        android:id="@+id/LoginActivity"
        android:name="com.example.bookinside.LoginActivity"
        android:label="@string/login_fragment_label"
        tools:layout="@layout/login_layout">

        <action
            android:id="@+id/action_Login_to_Register"
            app:destination="@id/RegisterActivity" />
        <action
            android:id="@+id/action_LoginActivity_to_forgetPasswordActivity"
            app:destination="@id/forgetPasswordActivity" />
    </fragment>

    <fragment
        android:id="@+id/RegisterActivity"
        android:name="com.example.bookinside.RegisterActivity"
        android:label="@string/register_fragment_label"
        tools:layout="@layout/register_layout">

        <action
            android:id="@+id/action_Register_to_Login"
            app:destination="@id/LoginActivity" />
    </fragment>

    <fragment
        android:id="@+id/forgetPasswordActivity"
        android:name="com.example.bookinside.ForgetPasswordActivity"
        android:label="forget_password_layout"
        tools:layout="@layout/forget_password_layout" >
        <action
            android:id="@+id/action_forgetPasswordActivity_to_LoginActivity"
            app:destination="@id/LoginActivity" />
    </fragment>
</navigation>
